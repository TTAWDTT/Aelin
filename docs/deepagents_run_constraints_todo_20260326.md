# DeepAgents Run Constraints Todo 2026-03-26

## 1. Agent Run 总约束

- [x] 增加整轮 run timeout，避免无限思考。
- [x] 增加单次工具调用超时，避免某个工具卡死拖住整轮。
- [x] 增加连续无增益工具调用上限，超过后强制收敛回答。
- [x] 增加同工具重复尝试上限，阻止模型在同一路径上打转。
- [x] 增加连续工具失败上限，失败过多后停止工具链。
- [x] 为工具拒绝结果设计统一的、可恢复的错误文案，让模型能修正而不是乱试。
- [x] 将“停止继续调工具，转为基于现有证据回答”的规则写入主逻辑。

## 2. System Prompt 工具调用策略

- [x] 明确要求：调用工具前必须先形成完整参数。
- [x] 明确要求：参数缺失时优先修正参数，不要继续盲试。
- [x] 明确要求：同类工具不要重复提交近似相同请求。
- [x] 明确要求：工具失败后最多进行有限次修正，不允许无限重试。
- [x] 明确要求：没有新信息时必须停止工具调用并总结当前结果。
- [x] 明确要求：若工具未成功，不得假装已完成搜索、读取、打开或操作。

## 3. web_search 工具策略

- [x] 强制 query 非空且有效，空 query 直接拒绝。
- [x] 对 query 做规范化去重，阻止近似重复搜索。
- [x] 增加重复搜索但无新结果的判定逻辑。
- [x] 增加 web_search 单工具最大调用次数。
- [x] 增加 web_search 失败后的收敛提示，让模型改为总结已有结果。
- [x] 明确 search 与 search_and_fetch 的使用边界，避免无意义升级调用。

## 4. attachment_search 工具策略

- [x] 强制 query 非空。
- [x] 强制 attachment 范围合法，超出本轮可用附件直接拒绝。
- [x] 阻止相同 query 在同一批附件上重复搜索。
- [x] 增加连续无命中时的收敛规则。
- [x] 明确命中不足时应回答“附件中未找到足够证据”，而不是反复试。

## 5. google_workspace 工具策略

- [x] 按 action 分类校验必填参数。
- [x] 对读操作与写操作分别设置更细的重试和失败规则。
- [x] 写操作失败时禁止自动反复重试。
- [x] 缺少授权或配置时返回可恢复错误，要求模型停止该工具链。
- [x] 增加 google_workspace 单工具调用上限。

## 6. device 工具策略

- [x] 按 status/open_url/open_aelin 分别校验参数。
- [x] open_url 必须是合法 URL，否则直接拒绝。
- [x] 阻止重复 open_url/open_aelin 调用。
- [x] 对设备类操作增加更强的单轮次数限制。
- [x] 失败后要求模型改为解释无法执行，而不是继续尝试。

## 7. screen_get 工具策略

- [x] 校验截图参数合法性。
- [x] 阻止短时间内重复截图。
- [x] 限制同轮截图次数。
- [x] 截图失败后要求模型停止重试并说明原因。

## 8. 后端工具事件契约

- [x] 不再把未成型的 tool-call 草稿态直接视为正式执行事件。
- [x] 只在 tool_call_id、name、args 达到稳定条件后再对外暴露。
- [x] 为 execution pane 提供稳定的工具运行事件，而不是直接依赖 message 草稿。
- [x] 将 tool pending/running/completed/error 状态统一化。
- [x] 明确草稿态消息和正式工具调用的边界。

## 9. 前端 Execution Pane

- [x] 按 tool_call_id 去重，只保留每次工具调用的最新状态。
- [x] 过滤无稳定 id、无有效 name、无有效 args 的占位项。
- [x] args 为空时不再展示误导性的 {}。
- [x] 将同类重复 pending 项合并展示。
- [x] 让工具面板优先展示稳定态和最终态，而不是所有中间态。

## 10. 联调与验收

- [x] 构造正常 web_search 调用真实链路测试。
- [x] 构造空参数工具调用被拒绝后自动修正的真实链路测试。
- [x] 构造重复搜索被阻止并收敛回答的真实链路测试。
- [x] 构造工具超时后整轮收敛退出的真实链路测试。
- [x] 构造 execution pane 不再出现一长串空 PENDING 的前端验收测试。
