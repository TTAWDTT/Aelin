# Browser Plane Skill for DeepAgents

本技能描述如何通过 `plane` 工具委派浏览任务给浏览器 plane（如 pinchtab）。

## 能力概览

- 打开指定 URL，并在可见浏览器中进行交互。
- 读取页面内容，总结要点或提取结构化信息。
- 处理需要验证码 / 登录的页面：通过用户在本地浏览器完成操作后继续任务。

## goal 模板示例

在构造 `goal` 时，要尽量包含以下信息：

- 要访问的 URL 或站点。
- 关注的页面区域或信息类型。
- 输出形式（要点列表 / 段落总结 / 表格等）。

示例：

- 「打开 https://www.baidu.com 并总结首页要闻，大致说明 3～5 条今日热点。」
- 「打开某电影网站的热门电影页，列出评分最高的前 10 部电影及其评分。」
- 「登录我的网银主页，检查本月账单金额，并用一句话告诉我是否有异常消费。」

## 使用约定

1. 使用 `plane` 工具并设置：
   - `plane = "browser"`
   - `action` 之一：
     - `delegate`：创建新的浏览任务。
     - `status`：查询任务状态。
     - `continue`：在任务仍在运行时继续执行。
     - `close`：在任务完成或用户不再需要时关闭任务。

2. 在判断是否需要 **继续已有任务** 时，可以综合考虑：
   - 用户最近一轮 query 是否提到「继续 / 接着 / 刚才 / 上一个任务」。
   - 是否存在 owner 相同、`state in {"running", "waiting_user"}` 的 plane task。
   - 若满足以上条件，优先对该 task 调用 `status` / `continue`，而不是创建新任务。

3. 当 `status` 返回：
   - `state = "running"`：继续轮询或调用 `continue`，直到任务进入 `completed` 或需要用户交互。
   - `state = "waiting_user"`：说明需要用户在可见浏览器中完成登录 / 验证码等步骤。此时应：
     - 向用户说明需要的操作内容（例如「请在刚刚弹出的浏览器中完成登录」）。
     - 提示用户完成后回复简短确认（例如「已登录，继续」）。
     - 等待用户确认后，再继续对同一 `task_id` 调用 `status` / `continue`。
   - `state = "completed"`：
     - 从 `summary` / `last_text` 中提取关键信息，为用户生成最终回答。
     - 如果用户不再需要，可酌情调用 `close` 释放资源。

4. 不要在没有必要时频繁创建新的浏览任务：
   - 如果已有同一 owner 的活跃任务（同一用户 / workspace / plane），
     并且用户 query 明显是在「接着刚才的操作」，应优先使用 `status` / `continue` 来续上。
   - 只有在用户提出一个全新的目标（与现有 `goal` 明显不同）时，才使用 `delegate` 创建新任务。
